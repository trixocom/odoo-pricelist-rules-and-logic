<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Heredar la vista de formulario de items de lista de precios -->
        <record id="product_pricelist_item_form_view_inherit" model="ir.ui.view">
            <field name="name">product.pricelist.item.form.inherit</field>
            <field name="model">product.pricelist.item</field>
            <field name="inherit_id" ref="product.product_pricelist_item_form_view"/>
            <field name="arch" type="xml">
                <!-- Agregar campos al inicio del formulario usando xpath -->
                <xpath expr="//form/sheet" position="inside">
                    <group name="and_logic_group" string="Lógica AND">
                        <field name="apply_and_logic"/>
                        <field name="and_group" invisible="not apply_and_logic"/>
                    </group>
                </xpath>
            </field>
        </record>

        <!-- Heredar la vista de árbol de items de lista de precios -->
        <record id="product_pricelist_item_tree_view_inherit" model="ir.ui.view">
            <field name="name">product.pricelist.item.tree.inherit</field>
            <field name="model">product.pricelist.item</field>
            <field name="inherit_id" ref="product.product_pricelist_item_tree_view"/>
            <field name="arch" type="xml">
                <!-- Agregar campos a la vista de árbol -->
                <xpath expr="//tree" position="inside">
                    <field name="apply_and_logic" optional="show"/>
                    <field name="and_group" optional="show"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
